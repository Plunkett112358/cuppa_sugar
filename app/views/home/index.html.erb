<% @items.each do |item| %>

  <%= item.name %>
  <%= item.category %>
  <%= item.description %>

  <% end %>

<% @user.each do |user| %>

<img src="<%= user.image_url %>"/>
  <%= user.name %>
  <%= user.bio %>
  <%= user.city %>
  <%= user.state %>

  <% end %>

