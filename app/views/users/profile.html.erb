<section id='user-info'>


   <img style= "width: 100px"src="http://www.freeartstutor.com/wp-content/uploads/2009/12/book-drawing-lessons-0005-1024x813.jpg" alt="">

   <img style= "width: 150px" src="<%= current_user.image_url %>"/> 

   <img style= "width: 100px"src="http://www.freeartstutor.com/wp-content/uploads/2009/12/book-drawing-lessons-0005-1024x813.jpg" alt=""></br>
  <%= current_user.name %></br>
  <%= current_user.bio %></br>
  <%= current_user.city %>,
  <%= current_user.state %>

 </section>


<!-- my borrows table
 -->

<h1>Books I've Borrowed</h1>
<table class="table table-bordered table-hover">
<section id='user-borrows'>
<thead>
  <tr>
      <td>Title</td>
      <td>Author</td>
      <td>Description</td>
      <td>Date</td>
      <td>Returned?</td>
    </tr>

</thead>
<tbody>
  <tr>
  <% @my_borrows.each do |borrow| %>

    <td><%= borrow.item.name%></td>
    <td><%= borrow.item.author %></td>
    <td><%= borrow.item.description %></td>
    <td><%= borrow.created_at.strftime("Borrowed on %m/%d/%Y") %></td>
     <td> <%=borrow.item.borrowed%> </td>
<% end %>
<tr>
</tbody></table>
</section> 




<!-- My Items Currently Being borrowed
 -->

 <h1>Books of Mine Currently Being Borrowed</h1>
<table class="table table-bordered table-hover">
<section id='user-borrows'>
<thead>
  <tr>
      <td>Title</td>
      <td>Author</td>
      <td>Description</td>
      <td>Currently in the Possesion of</td>
      <td>Date</td>
      <td>Returned?</td>
      <td>Send a Text Message Reminder</td>
    </tr>

</thead>
<tbody>
  <tr>
  <% @my_lends.each do |lend| %>

    <td><%= lend.item.name%></td>
    <td><%= lend.item.author %></td>
    <td><%= lend.item.description %></td>
    <td><%= lend.item.user.name %></td>
    <td><%= lend.created_at.strftime("Borrowed on %m/%d/%Y") %></td>
    <td><%= link_to "Returned", borrow_path(lend), method: "delete" %></td>
    <form><td> <button>Remind</button> </td></form>
<tr>
<% end %>

</tbody></table>
</section> 







<!-- things I own -->

<h1>Books I Own</h1>
<!-- add an item -->



<h4>(Add a new Item)</h4>
<%= form_for @new_item do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %> 

 <%= f.label :author %>
  <%= f.text_field :author %>

   <%= f.label :description %>
  <%= f.text_field :description %>  

<%= f.submit :submit, class: 'btn btn-primary' %>
<%end %>


<!-- add an item -->

<section id='user-items'>
<table class="table table-bordered table-hover">
<thead>
  <tr>
      <td>Title</td>
      <td>Author</td>
      <td>Description</td>
      <td>Already Borrowed?</td>
      <td>Delete</td>
      
    </tr>
</thead>

<tbody>
  
  <% @items.each do |item| %>
<tr>
    <td><%= item.name %></td>
    <td><%= item.author %></td>
    <td><%= item.description %></td>
    <td><%=item.borrowed%></td>
    <td><%= link_to "Delete", item_path(item), method: "delete" %></td>

  <% end %>
</tr>
</tbody>

</table>
 </section>


