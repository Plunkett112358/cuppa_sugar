

<section id='user-items'>
<table class="table table-bordered table-hover">
<thead>
  <tr>
      <td>Name</td>
      <td>Author</td>
      <td>Description</td>
       <td>Owner</td>
       <td>Distance</td>
       <td>Ask to Borrow</td>

  
    </tr>
</thead>

<tbody>
  
  <% @result.each do |result| %>
<tr>
    <td><%= result.name %></td>
     <td><%= result.author %></td>
    <td><%= result.description %></td>
    <td><%= result.user.name %></td>
    <td>[Distance goes here]</td>

    <!-- Borrow.new(item_id: params[:id], user_id: current_user) -->
    <form method><td> <button>Borrow!</button> </td></form>

  <% end %>
</tr>
</tbody>

</table>
 </section>
